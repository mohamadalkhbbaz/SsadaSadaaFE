import React from 'react'

const FirstComp = (props) => {
  return (
    <div>
        <h1>FirstComp :</h1>
        <p>{props.testFirstParameter}</p>
        <hr></hr>
    </div>
  )
}

export default FirstComp


// you can name instead props any name >>> like ssada
